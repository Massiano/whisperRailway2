--extra-index-url https://download.pytorch.org/whl/cpu

# PyTorch CPU-only (use plain version — no +cpu suffix)
torch==2.4.0
torchaudio==2.4.0
# torchvision optional; omit if not needed
# torchvision==0.19.0

# Core
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6

# Whisper (Hugging Face)
transformers>=4.36.0,<4.58
accelerate>=0.25.0
huggingface-hub>=0.25.0

# Audio loading (lightweight, no system ffmpeg)
librosa>=0.10.0
soundfile>=0.12.0
